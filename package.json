{
  "name": "@process-engine/consumer_api_meta",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "meta exec \"npm run build\" --exclude consumer_api_meta,consumer_api_contracts && cd _integration_tests && npm run build && cd ..",
    "postinstall": "minstall --trust-local-modules ."
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "meta-npm": "0.0.18",
    "minstall": "^3.3.1-pre3"
  }
}
